server {
    listen 80;
    server_name scanner.livosys.se;

    # Redirect HTTP to HTTPS (activate after certbot)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass         http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }
}

# Uncomment this block AFTER running: certbot --nginx -d scanner.livosys.se
# server {
#     listen 443 ssl;
#     server_name aktier.livosys.se;
#
#     ssl_certificate     /etc/letsencrypt/live/scanner.livosys.se/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/scanner.livosys.se/privkey.pem;
#     include             /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass         http://127.0.0.1:3001;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 30s;
#     }
# }
